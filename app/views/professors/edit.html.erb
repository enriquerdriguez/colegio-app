<div class="row">
  <%= render 'shared/errors', obj: @professor %>
  <h2>Edit <%= @professor.first_name + " " + @professor.last_name %></h2>
  <div class="col-md-8">
    <%= render 'shared/student-professor-form', obj: @professor %>
  </div>
  <div class="col-md-offset-4"></div>
</div>
